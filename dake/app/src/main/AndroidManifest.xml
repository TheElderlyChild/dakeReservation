<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elderlyChild.dake">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/dakeTheme">
        <activity android:name=".ui.accounts.RewardsActivity"></activity>
        <activity android:name=".ui.accounts.ProfileActivity" />
        <activity android:name=".ui.accounts.OrdersActivity" />
        <activity android:name=".ui.accounts.NotificationsActivity" />
        <activity android:name=".ui.accounts.HelpActivity" />
        <activity android:name=".ui.accounts.AboutUsActivity" />
        <activity android:name=".ui.accounts.AccountActivity" />
        <activity android:name=".ui.orders.RestaurantDetailsActivity" />
        <activity android:name=".ui.search.SearchActivity" />
        <activity android:name=".ui.HomePageActivity" />
        <activity android:name=".ui.AuthenticationActivity" android:theme="@style/dakeTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>